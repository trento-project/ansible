This example portrays a single-node configuration, where all components are
deployed on the same machine. In this deployment:
 - `vitellone` is the hostname where every component gets deployed

Additionally, the example shows how to configure optional parameters
like alerting configuration.
