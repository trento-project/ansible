# code: language=ansible
---
- name: Close prometheus port using firewalld
  ansible.builtin.command: >
    firewall-cmd --permanent --remove-port={{ prometheus_port }}/tcp
  when: firewalld_installed
  notify:
    - Restart firewalld if running
