# code: language=ansible
---
# Cleanup tasks for Prometheus: stop and disable the service. 

- name: Stop and disable prometheus
  ansible.builtin.service:
    name: prometheus
    state: stopped
    enabled: false
